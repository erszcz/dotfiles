#!/bin/sh

WHERE="apps deps include* src* test*"
CTAGS=ctags

[[ -f tags ]] && rm tags
find ${WHERE} \
    -name .git -prune -o \
    -name '*.erl' -print -o \
    -name '*.hrl' -print -o \
    -name '*.c' -print -o \
    -name '*.h' -print \
    | ${CTAGS} -L-
